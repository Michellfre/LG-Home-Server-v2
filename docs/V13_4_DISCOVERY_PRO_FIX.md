# v13.4 Discovery PRO Fix

Correções:
- Corrigido `network_list is not defined`.
- Corrigido uso de variáveis globais automáticas do DOM.
- Todas as chamadas agora usam `document.getElementById`.
- Discovery continua em background com `/api/discovery/start`.
- Estado em tempo real via `/api/discovery/state`.
