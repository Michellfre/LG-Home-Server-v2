# V13.5 — RTSP Camera Setup

A câmera só é marcada como configurada quando o `ffprobe` consegue abrir o stream.

O sistema não tenta descobrir ou adivinhar senhas. Use as credenciais definidas no aplicativo ou na etiqueta/manual da sua própria câmera.
